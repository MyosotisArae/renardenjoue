{% extends "main.html.twig" %}

{% block entete %}
  {{ parent() }}
  <link rel="stylesheet" href="{{css_path}}accueil.css">
<style>
  .encadrer
  {
    box-sizing: border-box;
    background : Ivory;
    opacity : 0.9;
    padding-right : 1em;
    padding-left : 1em;
  }
</style>
{% endblock %}

{% block laPage %}

  <div class="col-12">
    <br>
  </div>

  <table>
    <tr>
      <td class="largeur2tiers">{# colonne 1 #}
  <div class="container" style="box-sizing: border-box; padding-right : 5em;">{# colonne 1 #}

      <table>
        <tr>
          <td> <img src="{{img_path}}renard_vectoriel.svg" alt="Le Renard Enjoué" style="width : 10vw;"> </td>
          <td> <h1>Le Renard Enjoué</h1> </td>
        </tr>
      </table> {# fin : titre du message #}

      <table>
        <tr>
          <td>Bienvenue sur le site de notre association. Vous voulez jouer ? Vous êtes au bon endroit. </td>
        </tr>
        <tr>
          <td> &nbsp; </td>
        </tr>
        <tr>
          <td align="justify">Notre association organise des séances de jeux de société sur Bussy Saint Georges. Vous les retrouverez sur <a href="calendrier">l'agenda</a>, où vous pourrez vous y inscrire. Vous pouvez aussi le faire depuis <a href="https://discord.com/channels/886954892076404768/886957613110153226">notre forum.</a>
          </td>
        </tr>
        <tr>
          <td> &nbsp; </td>
        </tr>
        <tr>
          <td> On n'attend plus que vous ! </td>
        </tr>
        <tr>
          <td> &nbsp; </td>
        </tr>
      </table>

  </div>{# colonne 1 #}
      </td>{# colonne 1 #}

      <td>{# colonne 2 #}
  <div"> {# Colonne 2 #}

      <div class="cadreBleu fondOpaque">
        <i class="fas fa-calendar-alt fa-2x"></i>&nbsp;Nos prochains évènements :<br>
        <ul>
        {% if nextEvts|length > 0 %}
          {% for evt in nextEvts %}
          <li>Le {{evt.date}} :
          {% set titre = evt.titre %}
          {% if evt.capacite > 0 %}
            {% set titre = "Aprèm-jeux de l'asso" %}
          {% endif %}
          {#<div style='margin-left : 2.5em;'><a href='afficherEvt{{evt.id}}'>{{titre}}</a></div></li>#}
          <a href='afficherEvt{{evt.id}}'>{{titre}}</a></li>
          {% endfor %}
    {% else %}
      <br>Rien n'est prévu dans les prochains mois.<br>
        {% endif %}
        </ul>
      </div> {# Fin du cadre des evts #}

    <div> {# On saute une ligne entre les deux cadres #}
      <br>
    </div>

      <div class="cadreBleu fondOpaque">{# derniers jeux #}
        <i class="fas fa-dice fa-2x"></i>&nbsp;Les nouveaux jeux de notre collection :<br>
        <ul>
        {% for jeu in lastGames %}
        <li><a href="choisirJeu{{jeu.id}}">{{jeu.nomComplet}}</a></li>
        {% endfor %}
        </ul>
        <br>
        Venez les tester !<br>
      </div> {# cadre : derniers jeux #}

  </div> {# fin : Colonne 2 #}
      </td>{# colonne 2 #}
    </tr>
  </table>

{% endblock %} {# Fin du block laPage #}

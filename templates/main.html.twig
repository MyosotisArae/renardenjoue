<!DOCTYPE html>
<html>
<head>
{% block entete %}
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link rel="stylesheet" href="{{ BScss_path }}" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel="stylesheet" href="{{css_path}}general.css">

  <script src="{{ BSjs_path }}" integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM" crossorigin="anonymous"></script>
  <script src="https://kit.fontawesome.com/ac0e3984ff.js"></script>

  <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
  <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
  <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.3/html5shiv.min.js"></script>
  <![endif]-->
  
{% endblock %}
</head>

  <body>
{% block corps %}
  {% set isAdministrateur = false %}
  {% set afficherConnexion = false %}

    <header class="row">
      <div class="col-12 container-fluid">
        <nav class="navbar navbar-expand-sm navbar-dark bg-primary">
            <a class="navbar-brand col-1" href="#"><img src="{{img_path}}logo.gif" class="logo" class="enAbsolu" style="top:-1em;left:0;"></a>
            <div class="navbar-nav col-11">
              <div class="d-none d-md-block"> {# Menu pour grand ecran #}
                <li class="nav-item{{ ongletActif("ACC") }}">
                  <a class="nav-link" href="accueil"><i class="fas fa-home">&nbsp;</i>Accueil <span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item{{ ongletActif("CAL") }}">
                  <a class="nav-link" href="calendrier"><i class="fas fa-calendar-alt">&nbsp;</i>Agenda</a>
                </li>
                <li class="nav-item{{ ongletActif("JEU") }}">
                  <a class="nav-link" href="jeux"><i class="fas fa-dice">&nbsp;</i>Jeux</a>
                </li>
                {% if isAdministrateur %}
                <li class="nav-item">
                  <select class="nav-link bg-primary">
                    <option>Administrateur</option>
                    <option>Evènements</option>
                    <option>Jeux</option>
                  </select>
                </li>
                {% endif %}
                {% if afficherConnexion %}
                <li class="nav-item{{ ongletActif("CNX") }}">
                  <a class="nav-link" href="login"><i class="fas fa-sign-in-alt">&nbsp;</i>Se connecter</a>
                </li>
                <li class="nav-item{{ ongletActif("CMP") }}">
                  <a class="nav-link" href="compte"><i class="fas fa-address-card">&nbsp;</i>Mon compte</a>
                </li>
                {% endif %}
                <li class="nav-item{{ ongletActif("CTC") }}">
                  <a class="nav-link" href="contact"><i class="fas fa-comment">&nbsp;</i>Contact</a>
                </li>
              </div> {# Menu pour grand ecran #}
              <div class="navbar-nav col-xs-11 d-md-none"> {# Menu pour petit ecran #}
                <li class="nav-item{{ ongletActif("ACC") }}" title="Page d'accueil">
                  <a class="nav-link" href="accueil"><i class="fas fa-home">&nbsp;</i><span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item{{ ongletActif("CAL") }}" title="Calendrier des évènements">
                  <a class="nav-link" href="calendrier"><i class="fas fa-calendar-alt">&nbsp;</i></a>
                </li>
                <li class="nav-item{{ ongletActif("JEU") }}" title="Nos jeux">
                  <a class="nav-link" href="jeux"><i class="fas fa-dice">&nbsp;</i></a>
                </li>
                {% if isAdministrateur %}
                <li class="nav-item">
                  <select class="nav-link bg-primary">
                    <option>Administrateur</option>
                    <option>Evènements</option>
                    <option>Jeux</option>
                  </select>
                </li>
                {% endif %}
                {% if afficherConnexion %}
                <li class="nav-item{{ ongletActif("CNX") }}">
                  <a class="nav-link" href="login"><i class="fas fa-sign-in-alt">&nbsp;</i></a>
                </li>
                <li class="nav-item{{ ongletActif("CMP") }}">
                  <a class="nav-link" href="compte"><i class="fas fa-address-card">&nbsp;</i></a>
                </li>
                {% endif %}
                <li class="nav-item{{ ongletActif("CTC") }}" title="Formulaire de contact">
                  <a class="nav-link" href="contact"><i class="fas fa-comment">&nbsp;</i></a>
                </li>
              </div> {# Menu pour petit ecran #}
            </div>
        </nav>
      </div>
    </header>

    <img class="fond container-fluid d-none d-md-block enAbsolu" style="top:3em;background-size : cover;" src="{{img_path}}fond.png">
    <img class="fond container-fluid d-md-none" style="top:3em;background-size : cover;" src="{{img_path}}fondVertical.png">

    <div class="row">
      <div class="col-12">
          {% block laPage %}
          {% endblock %}
      </div> {# contenant des pages #}
    </div> {# div principale #}

{% endblock %}
{% block piedDePage %}
{% endblock %}
  </body>
</html>